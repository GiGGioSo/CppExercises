#pragma once

#include <iostream>
#include "bunny.h"

int addInFront(Node*& h);

int addInMiddle(Node*& h, int pos);

int addAtTheEnd(Node*& h); // magari aggiungi un puntatore alla coda, vedi tu

void printList(Node*& h);